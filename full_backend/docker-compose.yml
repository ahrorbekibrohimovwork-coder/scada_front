version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      # Важно для подключения к Ollama из Docker
      - OLLAMA_HOST=http://host.docker.internal:11434